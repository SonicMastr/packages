diff --git a/Makefile.vita b/Makefile.vita
index ce1a8ba..e2e5f0d 100644
--- a/Makefile.vita
+++ b/Makefile.vita
@@ -3,14 +3,14 @@
 TARGET_LIB = libSDL2_ttf.a
 OBJS = SDL_ttf.o
 
-PREFIX  = arm-vita-eabi
+PREFIX  = arm-dolce-eabi
 CC      = $(PREFIX)-gcc
 AR      = $(PREFIX)-ar
 
-CFLAGS  = -Wl,-q -Wall -O2 \
+CFLAGS  = -Wall -O2 \
 	-Wno-unused-but-set-variable \
-	-I$(VITASDK)/arm-vita-eabi/include/SDL2 \
-	-I$(VITASDK)/arm-vita-eabi/include/freetype2
+	-I$(DOLCESDK)/$(PREFIX)/include/SDL2 \
+	-I$(DOLCESDK)/$(PREFIX)/include/freetype2
 
 CFLAGS	+= -DPACKAGE_NAME=\"\" -DPACKAGE_TARNAME=\"\" -DPACKAGE_VERSION=\"\" -DPACKAGE_STRING=\"\" \
 		-DPACKAGE_BUGREPORT=\"\" -DSTDC_HEADERS=1 -DHAVE_SYS_TYPES_H=1 -DHAVE_SYS_STAT_H=1 \
@@ -25,7 +25,8 @@ clean:
 	@rm -rf $(TARGET_LIB) $(OBJS)
 
 install: $(TARGET_LIB)
-	@cp  $(TARGET_LIB) $(VITASDK)/arm-vita-eabi/lib
-	@mkdir -p "$(VITASDK)/arm-vita-eabi/include/SDL2"
-	@cp SDL_ttf.h "$(VITASDK)/arm-vita-eabi/include/SDL2"
+	@mkdir -p "$(DESTDIR)$(DOLCESDK)/$(PREFIX)/lib"
+	@cp  $(TARGET_LIB) "$(DESTDIR)$(DOLCESDK)/$(PREFIX)/lib"
+	@mkdir -p "$(DESTDIR)$(DOLCESDK)/$(PREFIX)/include/SDL2"
+	@cp SDL_ttf.h "$(DESTDIR)$(DOLCESDK)/$(PREFIX)/include/SDL2"
 	@echo "Installed!"
