diff --git a/Makefile.vita b/Makefile.vita
index 28f0486..7b50f5f 100644
--- a/Makefile.vita
+++ b/Makefile.vita
@@ -21,10 +21,10 @@ COMMON_OBJS = effect_position.o \
 	playmus.o \
 	playwave.o \
 
-PREFIX  = arm-vita-eabi
+PREFIX  = arm-dolce-eabi
 CC      = $(PREFIX)-gcc
 AR      = $(PREFIX)-ar
-CFLAGS  = -Wl,-q -Wall -O3 -I$(VITASDK)/arm-vita-eabi/include/SDL2 -DMUSIC_OGG -DMUSIC_MOD_MIKMOD -DMUSIC_WAV -DMUSIC_MP3_MPG123 -DMUSIC_FLAC
+CFLAGS  = -Wall -O3 -I$(DOLCESDK)/$(PREFIX)/include/SDL2 -DMUSIC_OGG -DMUSIC_MOD_MIKMOD -DMUSIC_WAV -DMUSIC_MP3_MPG123 -DMUSIC_FLAC
 ASFLAGS = $(CFLAGS)
 
 LIBS    += -lSDL2
@@ -36,8 +36,9 @@ clean:
 	@rm -rf $(TARGET_LIB) $(COMMON_OBJS)
 
 install: $(TARGET_LIB)
-	@cp  $(TARGET_LIB) $(VITASDK)/arm-vita-eabi/lib
-	@mkdir -p "$(VITASDK)/arm-vita-eabi/include/SDL2"
-	@cp SDL_mixer.h "$(VITASDK)/arm-vita-eabi/include/SDL2"
+	@mkdir -p "$(DESTDIR)$(DOLCESDK)/$(PREFIX)/lib"
+	@cp  $(TARGET_LIB) "$(DESTDIR)$(DOLCESDK)/$(PREFIX)/lib"
+	@mkdir -p "$(DESTDIR)$(DOLCESDK)/$(PREFIX)/include/SDL2"
+	@cp SDL_mixer.h "$(DESTDIR)$(DOLCESDK)/$(PREFIX)/include/SDL2"
 	@echo "Installed!"
 
